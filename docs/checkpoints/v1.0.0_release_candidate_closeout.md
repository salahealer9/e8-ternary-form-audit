# v1.0.0 release-candidate closeout

## Status

**STATUS: V1.0.0_RELEASE_CANDIDATE_REPRODUCTION_PASS**

The public-release candidate has been reproduced from a fresh clone outside
the development working tree.

No dependency on the existing development virtual environment, Python caches,
editable-install metadata, or other local generated state was required.

## Audited source commit

Release-candidate source commit:

```text
48956db68c0ebc6e1b3a3fb6b9fa5741fad77d3d
```

This commit includes:

- manuscript version 1.0;
- audited bibliography and prior-art record;
- final referee-style manuscript audit;
- public-facing repository metadata;
- `CITATION.cff`;
- dual licensing;
- methodology documentation;
- exact derivation-chain documentation;
- canonical machine-readable tensor artifact.

## Environment

Fresh-clone validation environment:

```text
Python: 3.11.2
pytest: 9.1.1
NumPy: 2.4.6
```

The project was installed from the clean clone using:

```bash
python -m pip install -e ".[dev]"
```

## Full automated test suite

The clean checkout collected:

```text
108 tests
```

Result:

```text
108 passed
```

No test failures occurred.

## Exact phase reproduction

The principal deterministic phase runners were executed in dependency order.

### Phase 1B — independent E8 validation

Result:

```text
roots: 240
exact rank: 8
crystallographic integrality: PASS
reflection closure: PASS
nonorthogonality graph connected: PASS
```

Status:

```text
PHASE1B_INDEPENDENT_VALIDATION: PASS
```

### Phase 2A — zero-sum triples

Result:

```text
roots: 240
unordered zero-sum triples: 2240
ordered zero-sum triples: 13440
root incidence degree: 28
```

Status:

```text
PHASE2A_ZERO_SUM_TRIPLES: PASS
```

### Phase 2B — A2 quotient

Result:

```text
six-root classes: 1120
class multiplicity: 2
rank-2 subsystems: 1120
reflection-closed subsystems: 1120
structurally A2 subsystems: 1120
```

Status:

```text
PHASE2B_A2_QUOTIENT: PASS
```

### Phase 2C — ternary incidence

Result:

```text
vertices: 240
unordered hyperedges: 2240
ordered support: 13440
degree: 28
active unordered root pairs: 6720
negation orbits: 1120
```

The Phase 2C negation-orbit classes agree exactly with the independently
constructed Phase 2B A2 classes.

Status:

```text
PHASE2C_TERNARY_INCIDENCE: PASS
```

### Phase 3A — root-space Cartan support

Result:

```text
root-sector domain: 13824000
nonzero root-sector support: 13440
```

The Lie-derived root support agrees exactly with the Phase 2C Boolean support.

Status:

```text
PHASE3A_ROOT_SPACE_CARTAN_SUPPORT: PASS
```

### Phase 3B — full sector architecture

Result:

```text
Cartan dimension: 8
root spaces: 240
total Lie-algebra dimension: 248
RRR support: 13440
opposite-root lines: 120
```

All two-Cartan and three-Cartan sectors are forced zero.

Status:

```text
PHASE3B_FULL_SECTOR_SUPPORT: PASS
```

### Phase 3C — canonical basis and normalization

Result:

```text
simple-root rank: 8
positive roots: 120
negative roots: 120
height range: -29 .. 29
admissible root-root brackets: 13440
Chevalley sign spectrum:
    -1: 6720
    +1: 6720
```

The frozen epsilon convention and normalized opposite-root pairing pass their
exact consistency checks.

Status:

```text
PHASE3C_CANONICAL_BASIS_NORMALIZATION: PASS
```

### Phase 3D — signed sparse Cartan 3-form

The complete ordered basis domain is:

```text
248^3 = 15252992
```

Sparse support:

```text
RRR:   13440
HRR:    2736
total: 16176
```

Complete coefficient census:

```text
-2:   24
-1: 8064
+1: 8064
+2:   24
```

Two algorithmically distinct exact tensor constructions agree with:

```text
direct-only keys: 0
reconstruction-only keys: 0
differing coefficients: 0
```

Status:

```text
PHASE3D_SIGNED_SPARSE_CARTAN_FORM: PASS
```

## Canonical artifact integrity

Canonical artifact:

```text
data/derived/phase3d_signed_sparse_cartan_form.json
```

Frozen SHA-256:

```text
43b52b4116dfcf0dace332234b5c9dc50d95d51effb690aec8aebe39ad4f92e8
```

The hash was measured before regeneration and after regeneration.

Result:

```text
HASH DIFF: empty
```

Therefore the deterministic regeneration is byte-for-byte identical to the
tracked artifact.

## Phase 4A — independent Lie-algebra audit

Bracket domain:

```text
248^2 = 61504
```

Nonzero ordered bracket pairs:

```text
15504
```

Exact bracket properties:

```text
antisymmetry: PASS
self-brackets zero: PASS
closure: PASS
```

Exhaustive Jacobi verification:

```text
HHH:       56 checked, 0 failures
HHR:     6720 checked, 0 failures
HRR:   229440 checked, 0 failures
RRR:  2275280 checked, 0 failures
---------------------------------
total: 2511496 checked, 0 failures
```

The independently reconstructed invariant form is symmetric and
nondegenerate, and its exact invariance identity holds.

The algebra-derived Cartan 3-form has:

```text
16176 nonzero entries
```

with coefficient census:

```text
-2:   24
-1: 8064
+1: 8064
+2:   24
```

and agrees entry-for-entry with the sealed Phase 3D artifact.

Status:

```text
PHASE4A_LIE_ALGEBRA_IDENTITY_AUDIT: PASS
```

## Phase 6B — literal ternary realization

The exact callable realizes

\[
g(v_1,v_2,v_3)=B_0(v_1,[v_2,v_3]).
\]

The two exact evaluation routes agree on arbitrary rational vectors.

The basis tensor contains:

```text
16176 nonzero coefficients
```

with complete value set:

```text
[-2, -1, 0, 1, 2]
```

Bracket recovery was tested on all:

```text
61504
```

ordered basis pairs.

Result:

```text
recovered-only pairs: 0
frozen-bracket-only pairs: 0
coefficient/vector mismatches: 0
exact bracket recovery: PASS
```

Status:

```text
PHASE6B_LITERAL_NOTATION_REALIZATION: PASS
```

## Manuscript artifact

Manuscript:

```text
paper/e8_cartan_3form_manuscript_v1.0.pdf
```

SHA-256 measured at the release-candidate checkpoint:

```text
7a2949350569d1339966132d8a292aacfe09332cca5bc7117cf3a5a17076f1a7
```

## Clean-tree result

After the complete test suite, deterministic phase executions, and canonical
artifact regeneration:

```text
git status --short
```

returned no output.

The fresh checkout therefore remained clean.

## Release conclusion

The mathematical and computational release candidate reproduces successfully
from a clean checkout.

The following conditions are satisfied:

- complete automated test suite passes;
- all principal deterministic phase runners pass;
- analytic headline counts are independently reproduced;
- canonical tensor artifact regenerates byte-for-byte;
- canonical tensor SHA-256 is unchanged;
- exhaustive Jacobi verification has zero failures;
- invariant-form reconstruction agrees exactly;
- sealed tensor reconstruction agrees exactly;
- complete bracket recovery has zero discrepancies;
- no generated change remains in the clean checkout.

No mathematical or computational release blocker remains at this checkpoint.

The remaining operations are release-administrative:

1. commit this release-candidate closeout;
2. verify the exact final commit;
3. make the GitHub repository public;
4. connect/enable the repository for Zenodo archival;
5. create the frozen `v1.0.0` GitHub release;
6. archive the release with Zenodo and obtain the DOI;
7. insert the repository URL, tag, release commit, DOI, and frozen tensor hash
   into the final manuscript;
8. rebuild the DOI-bearing manuscript and archive that final form.